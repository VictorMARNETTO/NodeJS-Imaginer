<section class="section">
    <div class="container is-fullhd">
        <form action="/user/loginAuth" method="POST" enctype="multipart/form-data">
            <div class="field">
                <label class="label">Login</label>
                <div class="control">
                    <input class="input" type="text" name="email" placeholder="email address">
                </div>
            </div>
            <div class="field">
                <label class="label">Password</label>
                <div class="control">
                    <input class="input" type="password" name="password" placeholder="">
                </div>
            </div>
            <div class="field">
                <label><a href="#"> Mot de passe oublié </a></label>
            </div>
            <div class="field is-grouped">
                <div class="control">
                    <button class="button is-link">Connexion</button>
                </div>
            
        </form>
        <form action="/" method="GET" enctype="multipart/form-data">
                <div class="control">
                    <button class="button is-text">Annuler</button>
                </div>
            </div>
        </form>
    </div>
</section>